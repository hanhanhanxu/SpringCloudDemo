server:
  port: 12000

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/hanhanxu/config.git
eureka:
  client:
    service-url:
      # ??eureka???,??
      defaultZone: http://localhost:10081/eureka